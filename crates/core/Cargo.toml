[package]
name = "huginn-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Reusable Copenhagen and multiverse hnefatafl rules API"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
pretty_assertions = "1"
serde_json = "1"

[lints]
workspace = true

